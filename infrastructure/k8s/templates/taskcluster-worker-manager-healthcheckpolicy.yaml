{{- if not (has "healthcheckpolicy" .Values.skipResourceTypes) -}}
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: taskcluster-worker-manager-hc
  labels:
    app.kubernetes.io/name: taskcluster-worker-manager-hc
    app.kubernetes.io/instance: '{{ .Release.Name }}'
    app.kubernetes.io/component: taskcluster-worker-manager-hc-healthcheckpolicy
    app.kubernetes.io/part-of: taskcluster
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        requestPath: /api/worker-manager/v1/ping
        port: 8080
  targetRef:
    group: ''
    kind: Service
    name: taskcluster-worker-manager
{{- end }}
