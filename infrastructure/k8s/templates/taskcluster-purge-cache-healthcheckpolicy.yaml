{{- if not (has "healthcheckpolicy" .Values.skipResourceTypes) -}}
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: taskcluster-purge-cache-hc
  labels:
    app.kubernetes.io/name: taskcluster-purge-cache-hc
    app.kubernetes.io/instance: '{{ .Release.Name }}'
    app.kubernetes.io/component: taskcluster-purge-cache-hc-healthcheckpolicy
    app.kubernetes.io/part-of: taskcluster
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        requestPath: /api/purge-cache/v1/ping
        port: 8080
  targetRef:
    group: ''
    kind: Service
    name: taskcluster-purge-cache
{{- end }}
