{{- if not (has "healthcheckpolicy" .Values.skipResourceTypes) -}}
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: taskcluster-secrets-hc
  labels:
    app.kubernetes.io/name: taskcluster-secrets-hc
    app.kubernetes.io/instance: '{{ .Release.Name }}'
    app.kubernetes.io/component: taskcluster-secrets-hc-healthcheckpolicy
    app.kubernetes.io/part-of: taskcluster
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        requestPath: /api/secrets/v1/ping
        port: 8080
  targetRef:
    group: ''
    kind: Service
    name: taskcluster-secrets
{{- end }}
