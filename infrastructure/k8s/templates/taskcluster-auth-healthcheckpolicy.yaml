{{- if not (has "healthcheckpolicy" .Values.skipResourceTypes) -}}
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: taskcluster-auth-hc
  labels:
    app.kubernetes.io/name: taskcluster-auth-hc
    app.kubernetes.io/instance: '{{ .Release.Name }}'
    app.kubernetes.io/component: taskcluster-auth-hc-healthcheckpolicy
    app.kubernetes.io/part-of: taskcluster
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        requestPath: /api/auth/v1/ping
        port: 8080
  targetRef:
    group: ''
    kind: Service
    name: taskcluster-auth
{{- end }}
