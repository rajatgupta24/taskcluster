apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: ${projectName}-hc
  labels: {$eval: labels}
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        requestPath: ${readinessPath}
        port: 8080
  targetRef:
    group: ""
    kind: Service
    name: ${projectName}
